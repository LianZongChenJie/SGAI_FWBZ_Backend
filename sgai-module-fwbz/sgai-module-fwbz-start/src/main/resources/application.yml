server:
  port: 7010
spring:
  application:
    name: sgai-tp
  config:
    import:
      - optional:nacos:jeecg.yaml
      - optional:nacos:fwbz-dev.yaml
  cloud:
    nacos:
      config:
        server-addr: 192.168.204.51:8848
        group: DEFAULT_GROUP
        namespace: public
        username:
        password:
      discovery:
        server-addr: ${spring.cloud.nacos.config.server-addr}
        namespace: public
        username:
        password:
        group: DEFAULT_GROUP
        ip: 192.168.204.206
    stream:
      bindings:
        springCloudBusInput:
          group: tp
mybatis-plus:
  #  configuration:
  #    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  type-handlers-package: org.jeecg.modules.fwbz.handler