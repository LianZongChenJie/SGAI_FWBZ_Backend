<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.fwbz.main.mapper.RealDataMapper">

    <select id="findFirstByTimeRangeAsc" resultType="org.jeecg.modules.fwbz.main.entity.RealData">
        SELECT *
        FROM (
        SELECT *,
        ROW_NUMBER() OVER (PARTITION BY device_id ORDER BY time ASC) AS rn
        FROM data_real
        WHERE time BETWEEN #{startTime} AND #{endTime}
        ) AS temp
        WHERE temp.rn = 1
    </select>

    <select id="findFirstByTimeRangeDesc" resultType="org.jeecg.modules.fwbz.main.entity.RealData">

        SELECT *
        FROM (
        SELECT *,
        ROW_NUMBER() OVER (PARTITION BY device_id ORDER BY time DESC) AS rn
        FROM data_real
        WHERE time BETWEEN #{startTime} AND #{endTime}
        ) AS temp
        WHERE temp.rn = 1
    </select>

    <select id="findFirstByLtTimeDesc" resultType="org.jeecg.modules.fwbz.main.entity.RealData">
        SELECT *
        FROM (
        SELECT *,
        ROW_NUMBER() OVER (PARTITION BY device_id ORDER BY time DESC) AS rn
        FROM data_real
        WHERE time BETWEEN #{startTime} AND #{endTime}
        ) AS temp
        WHERE temp.rn = 1
    </select>

</mapper>