server:
  port: 7201

spring:
  application:
    name: sgai-gather
  cloud:
    nacos:
      config:
        server-addr: 10.77.16.5:8848
        group: DEFAULT_GROUP
        namespace: public
        username:
        password:
      discovery:
        server-addr: ${spring.cloud.nacos.config.server-addr}
        group: ${spring.cloud.nacos.config.group}
        namespace: ${spring.cloud.nacos.config.namespace}
        username:
        password:
  config:
    import:
      - optional:nacos:jeecg.yaml
      - optional:nacos:gather-dev.yaml
  main:
    allow-bean-definition-overriding: true